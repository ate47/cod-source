set #x3111b4ccba20f3c5f 230
set #x346e64da56eedd1fc 130

//mantle on////////////////////

set #x3150f1bea8bd75190 0.4
set #x314ec11ea8bb0f3da 0.9

set #x34049b51456a7ee22 0.75

//need unique versions of thesee for mantle on!!!
//set #x3e17c73de66e23fa7 0.9  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
//set #x39b544887faae79e1 -40  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x3f0abd24877d9d9fe 0.4  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x34a0a7ec3b15e3c18 -10  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x33e2132c185018c8b 0.4  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x38af00b7ac9582acd -9  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x3a63f69db3fdfeb74 0.5	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x3ec79164e1aaaa372 -7  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x3833b40c300d999c 0.5	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x3826000fc374404da -6  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x349214af9b03e959d 0.6 	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x3bef099a7f654e773 -5  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions






//mantle over///////////////////
set #x3468a9a6bb67199fd 0.5
set #x34667a46bb64b6843 0.9

//set #x3e17c73de66e23fa7 0.3  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
//set #x39b544887faae79e1 -20  	//Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x338795444412529fd 0.2  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x321423d70a6d5f5d3 -20  	    //Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x3f816267e6f93eb50 0.2  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x31795aaa3bc2f6d26 -17  	    //Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x350db872a4259d99b 0.2  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x33084f3e8cb9172dd -15  	    //Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x380cf927538ee6e3 0.2  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x3afb77e2e273e0a65 -10  	    //Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions
set #x36cd0100045da7eaa 0.2  	    //Indicates where we place the bspline control point between the start and the ledge postions.  Rangel is 0-1 where a value of 0 would have the same xy coordinates of the 
set #x333cb39b4764baa8c -5 	    //Indicates how many units above the ledge positions we could place the bspline control point between the start and the ledge positions

set #x3cab4765eb36812f0 8				//indicates how many units we rise the third control point above the ledge position when mantling over.  When mantling up, the third control point is the ledge position
set #x31740cf9654f46cf6 0.6 		//Indicates where we place the bslpine control point between the ledge and the end positons( only used for mantle over, range is 0-1, where a value of 0 would have...
set #x3137fc4fc1dbdedf0 0 	//Indicates how many units above the ledge position we would place the bspline control point between the ledge and the edge positions

